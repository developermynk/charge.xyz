[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = false

# Arc Testnet — USDC is the native gas token.
[rpc_endpoints]
arc_testnet = "${ARC_TESTNET_RPC_URL}"
