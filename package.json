{
  "name": "charge-xyz",
  "version": "1.0.0",
  "private": true,
  "description": "Charge.xyz — DeFi at full charge. Swap, bridge, and deploy tokens across every major chain. pnpm + Turborepo monorepo.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5"
  },
  "pnpm": {
    "overrides": {
      "viem": "2.47.6"
    }
  }
}
